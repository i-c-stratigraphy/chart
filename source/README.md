# Source

The Python script `chart_remake.py` builds the file `chart.ttl` in the top-level directory by reading `chart-nolabels.ttl`
and adding in labels and definitions from `multilang.xlsx`.

It places interim results in `interim/`.

PREFIX skos: <http://www.w3.org/2004/02/skos/core#>

DELETE {
    ?age skos:prefLabel ?al .

    ?age skos:altLabel ?al .

    ?age skos:definition ?d .
}
WHERE {
    ?age
        a skos:Concept ;
        skos:prefLabel|skos:altLabel ?al ;
        skos:definition ?d ;
    .
}